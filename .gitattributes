# Techtyl - Git Attributes
# Ensures correct line endings for cross-platform development

# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF (Unix line endings)
*.sh text eol=lf

# Batch files use CRLF (Windows line endings)
*.bat text eol=crlf

# Markdown files
*.md text eol=lf

# JSON files
*.json text eol=lf

# PHP files
*.php text eol=lf

# Configuration files
.gitignore text eol=lf
.gitattributes text eol=lf
